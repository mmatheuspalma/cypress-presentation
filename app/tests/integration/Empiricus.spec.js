/// <reference types="Cypress" />


describe('Open Empiricus website', () => {
  beforeEach(() => {
    cy.viewport('macbook-15');
  });

  context('[WEBSITE]', () => {
    it('Try visit offers page', () => {
      // cy.visit();
    });
  });
});